# Keyline Studio backend — Python 3.11+
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Geospatial
rasterio>=1.4
rioxarray>=0.17
xarray>=2024.6
pyproj>=3.6
numpy>=2.0
scipy>=1.14
scikit-image>=0.24
shapely>=2.0
geopandas>=1.0
affine>=2.4
pillow>=10.0

# Hydrology (whitebox downloads its native binary on first init;
# pysheds is the pure-Python fallback)
whitebox>=2.3
pysheds>=0.4

# Boundary import/export (KML/KMZ) and map-scan PDFs
fastkml>=1.4
pypdfium2>=5.0

# Drone photogrammetry stack
pyodm>=1.5          # official NodeODM client
rq>=2.0             # durable job queue (worker runs as a separate process)
redis>=5.0
boto3>=1.34         # S3-compatible object storage (MinIO in dev)

# Tests
pytest>=8.0
httpx>=0.27
